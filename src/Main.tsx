import { h } from "hyperapp";

import "./styles/main.css";

export default () => (
  <section class="hero">
    <div class="container">
      <h1 class="title">AwesomeObserver⚡</h1>
    </div>
  </section>
);
